# Run project-wide type checks
echo "Checking types..."
npm run typecheck --prefix backend
npm run typecheck --prefix frontend

# Run backend unit tests
echo "Running backend tests..."
npm run test:run --prefix backend

# Lint staged files
npx lint-staged
