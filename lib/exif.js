// EXIF writing functionality
export const writeExif = async (mediaBuffer, options) => {
  // Placeholder for EXIF writing logic
  // For now, just return the mediaBuffer
  return mediaBuffer;
};

// Default export for backward compatibility
export default {
  writeExif,
};
