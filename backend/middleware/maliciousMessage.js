import { S_WHATSAPP_NET } from '@whiskeysockets/baileys';

export default async (ctx, context) => {
  // Placeholder for malicious message check
  // Since guaranteed_security is missing, we just allow all messages for now.
  return true;
};
