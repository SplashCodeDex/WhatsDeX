/**
 * WhatsDeX UI Component Library
 *
 * Atomic design system components - pure, stateless, and composable.
 * Import all UI primitives from this barrel file.
 *
 * @example
 * import { Button, Input, Card } from '@/components/ui';
 */

export { Button, buttonVariants, type ButtonProps } from './button';
