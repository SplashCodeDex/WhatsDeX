'use client';

import { AccountSettings } from '@/features/settings';

export default function SettingsPage() {
    return (
        <div className="p-8">
            <AccountSettings />
        </div>
    );
}
