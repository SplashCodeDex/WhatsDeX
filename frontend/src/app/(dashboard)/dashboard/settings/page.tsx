'use client';

import { WorkspaceSettings } from '@/features/settings';

export default function SettingsPage() {
    return (
        <div className="p-8">
            <WorkspaceSettings />
        </div>
    );
}
